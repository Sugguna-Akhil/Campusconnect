<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password - Campus Connect</title>
  <link rel="stylesheet" href="/forgotpassword.css">
</head>
<body>
  <header>
    <div class="logo">Campus Connect</div>
    <a href="/login" class="login-btn">Login</a> 
  </header>

  <div class="container">
    <% if (stage === "email") { %>
      <h2>Forgot Your Password?</h2>
      <% if (err) { %><div class="message">Email not found</div><% } %>
      <p>Enter your registered email. We’ll send a 6-digit OTP to reset your password.</p>
      <form action="/forgotpassword" method="post">
        <input type="email" name="email" placeholder="Enter your email" required>
        <button type="submit">Send OTP</button>
      </form>

    <% } else if (stage === "otp") { %>
      <h2>Enter OTP</h2>
      <% if (err) { %><div class="message">Invalid or expired OTP</div><% } %>
      <p>We sent an OTP to <b><%= email %></b>. Enter it below to verify.</p>
      <form action="/verifyotp" method="post">
        <input type="hidden" name="email" value="<%= email %>">
        <input type="text" name="otp" placeholder="Enter OTP" required>
        <button type="submit">Verify OTP</button>
      </form>

    <% } else if (stage === "reset") { %>
      <h2>Reset Password</h2>
      <p>Enter your new password below.</p>
      <form action="/resetpassword" method="post">
        <input type="hidden" name="email" value="<%= email %>">
        <input type="password" name="newPassword" placeholder="New Password" required>
        <button type="submit">Reset Password</button>
      </form>
    <% } %>

    <a href="/login">Back to Login</a>
  </div>
</body>
</html>

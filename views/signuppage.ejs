<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet" />
  <link href="/signup.css" rel="stylesheet" />
  <title>Sign Up</title>
</head>
<body>
  <nav class="navbar">
    <div class="logo">Campus Connect</div>
    <ul>
      <li><a href="/main">Home</a></li>
      <li><a href="/main">About</a></li>
      <li><a href="#">Contact</a></li>
    </ul>
    <a href="/login" class="login-btn">Log In</a>
  </nav>

  <div class="signup-container">
    <h2>Create your student account</h2>
    <p>Join the community and connect with fellow students!</p>

    <form action="/signup" method="post">
      <div class="form-group">
        <label for="fullname">Full Name</label>
        <input type="text" id="fullname" name="detail[name]" placeholder="John Doe" required />
      </div>
      <div class="form-group">
        <label for="email">Email address</label>
        <input type="email" id="email" name="detail[email]" placeholder="example@email.com" required />
      </div>
      <div class="form-group">
        <label for="department">College</label>
        <input type="text" id="department" name="detail[college]" placeholder="e.g., IIT Bombay" required />
      </div>
      <div class="form-group">
        <label for="year">Year of Study</label>
        <input type="text" id="year" name="detail[year]" placeholder="e.g., 2nd Year" required />
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="detail[password]" placeholder="Enter your password" required />
      </div>
      <button type="submit" class="btn-submit">Sign Up</button>
      <% if (typeof err !== 'undefined' && err) { %>
  <h5>Email ID already exists</h5>
<% } %>

    </form>

    <p>Already have an account? <a href="/login">Log in</a></p>
  </div>
</body>
</html>
